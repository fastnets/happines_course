# happines_course — V1.3.1 (Анкеты + админ библиотека + рандомная анкета всем)

## Запуск
1) `python -m venv .venv`
2) `.venv\Scripts\activate` (Windows)
3) `pip install -r requirements.txt`
4) Скопируй `.env.example` → `.env`, заполни BOT_TOKEN и DB_*
5) Запусти: `python main.py`

## Сброс dev БД
```sql
DROP TABLE IF EXISTS outbox_jobs, points_ledger, questionnaire_responses, questionnaires, quest_answers, progress, quests, lessons, enrollments, user_state, users CASCADE;
```

## Пользователь
- `/start` — меню
- `/enroll` — выбрать единое время доставки (ЧЧ:ММ)
- 🗓 Мой день → 📩 Получить материалы сейчас
- Лекция: описание + ссылка, кнопка **Просмотрено** (+баллы)
- Задание: ответ текстом (просто сообщением)
- После ответа на задание бот может (опционально) дать короткий поддерживающий фидбек + 1 уточняющий вопрос через GigaChat.
- Анкета: оценка 1–5 → бот спрашивает комментарий → сохраняет

## Админ
- `/admin` (только если TG id в ADMIN_TG_IDS)

## AI (GigaChat) — опционально
1) Заполни в `.env` переменные `GIGACHAT_BASIC` (Base64 Basic), `GIGACHAT_SCOPE`, `GIGACHAT_MODEL`.
2) Если ключи не заданы — бот работает без ИИ.
3) Баллы за задание начисляются сразу, ИИ — только дополнительная ценность.
- Библиотека → Анкеты:
  - Список, Создать, Редактировать, Удалить
  - 🎲 Рандомная анкета всем: вопрос → учитывать в диаграммах? → баллы → время ЧЧ:ММ
  - Рассылка анкеты не зависит от времени пользователя


## V1.3.2 добавлено
- Админка → Библиотека → Лекции: создать/редактировать/удалить
- Лекция всегда содержит ссылку на видео (поле video_url)
